import { ScrollView } from "react-native";
import React from "react";
import HeroBar from "../components/HeroBar";

const Home = () => {
  return (
    <ScrollView>
      <HeroBar />
    </ScrollView>
  );
};

export default Home;
